document.addEventListener("DOMContentLoaded", () => {
    const projectLink = document.querySelector(".project-nav-link");
    if (projectLink) {
        projectLink.setAttribute("aria-label", "Hunters: Fractura del Tiempo");
    }
});
